// lib/supabase/types.ts
//
// Placeholder — generate the real, fully-typed version once your schema is
// finalized (it already is, per your migrations) by running:
//
//   npx supabase login
//   npx supabase gen types typescript --project-id <your-project-ref> > lib/supabase/types.ts
//
// Find <your-project-ref> in the Supabase dashboard URL:
// https://supabase.com/dashboard/project/<project-ref>
//
// Re-run this command any time you add/change a table so client/server.ts's
// createClient<Database>() stays accurate and you get real autocomplete +
// type-checking on every .from('table_name') call instead of `any`.

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any
